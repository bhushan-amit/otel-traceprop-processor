receivers:
  otlp:
    protocols:
      grpc:

processors:
  tracepropagator: {}

exporters:
  debug: {}

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [tracepropagator]
      exporters: [debug]
